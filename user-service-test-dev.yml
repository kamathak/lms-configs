spring:
  profiles:
    active: dev
  application:
    name: user-service-test
    
  jpa:
    show-sql: false  

redis:
  configuration:
    secure: true
    
test: development
 
logging:
  level:
    com:
      cts:
        user: DEBUG

